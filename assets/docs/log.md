# 更新记录

## 060910
- 添加左下角小号数字版本号。
- 约定版本号格式为 `MMDDNN`，其中 `MMDD` 为日期，`NN` 为当天修改次数。

## 060911
- 教学流程新增第 12 步“模拟导入示例列表”。
- 添加模拟文件选择界面，包含假的 `example-list.json`，并用金光引导点选。
- 模拟导入只影响教学列表，不影响主页列表。
- 原第 12 步顺延为第 13 步，教学结语顺延。
- 原“导入示例列表”介绍改为“导出备份”。
- 清理已完成的待办项。

## 060912
- 正常模式导入时不再自动下载旧列表备份。
- 导入前旧列表改为临时保存在浏览器页面状态中。
- 导入新列表后、抽取前支持撤回导入操作。
- 用户抽取一次后自动清除可撤回的旧列表状态。

## 060913
- 教学第 12 步改为由用户自行点击汉堡菜单，再点击“导入”，之后才显示模拟文件选择界面。
- 教学第 12 步中仅临时开放“导入”按钮，其它教学步骤仍禁用导入。
- 版本号确认在欢迎页也显示。

## 060914
- 教学第 12 步模拟导入后，右侧教学列表支持上下滚动。
- 教学第 13 步介绍“导出备份”时，汉堡菜单和“导出”按钮都有金光提示。
- 修复欢迎页看不到版本号的问题，提高版本号层级。

## 060915
- 新建 `log.md` 保存版本更新记录。
- 教学第 13 步新增“类别改名”介绍，并高亮分类右侧“改名”按钮。
- 将原第 13 步内容拆分：第 13 步介绍抽取范围、类别改名、导出备份；第 14 步介绍搜索、全展开 / 全收起。
- 教学结语顺延为第 15 步。

## 060916
- 将“模拟导入分享列表”提前到点击确认结果之后，作为新的第 7 步。
- 原第 7 步及后续教学步骤整体顺延。
- 调整导入教学文案，删除“示例 / 不影响主页列表”的说明，改为强调接收别人分享的列表和跨设备迁移。

## 060917
- 修复退出教学模式后普通模式“抽一个”按钮可能保持禁用的问题。
- 普通模式下会明确恢复抽取按钮和结果确认按钮的可用状态。

## 060918
- 调整项目文件结构：根目录保留 `WhatToEat.html`，运行文件移动到 `assets/app/`，Markdown 文档移动到 `assets/docs/`。
- 更新页面资源引用路径。
